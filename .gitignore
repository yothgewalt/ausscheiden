node_modules/
build/
dist/
.next/
next-env.d.ts
coverage/
.DS_Store
*.log
.env*
!.env.example

# graft's local graph cache — regenerable, not committed (run `graft build`).
graft/

.playwright-mcp

.claude

# committed-by-mistake: real slip PII + app metadata — keep local, never track
transaction.json
metadata.json
